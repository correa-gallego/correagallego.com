---
import Base from '../layouts/Base.astro';
import { getCollection } from 'astro:content';

const outputs = (await getCollection('outputs')).sort((a, b) => a.data.order - b.data.order);
const manuscripts = outputs.filter((item) => item.data.kind === 'manuscript');
const presentations = outputs.filter((item) => item.data.kind === 'presentation');
const awards = outputs.filter((item) => item.data.kind === 'award');
const formatAuthors = (authors: string[]) =>
  authors.map((author) => (author === 'Sebastian Correa-Gallego' ? `<strong>${author}</strong>` : author)).join(', ');
---

<Base title="Publications — Sebastian Correa-Gallego" description="Current manuscripts, presentations, and recognitions from the current stage of my academic profile.">
  <header class="page-intro">
    <div class="container">
      <span class="page-intro__eyebrow">Publications</span>
      <h1>Current academic output</h1>
      <p class="page-intro__lede">
        This page is intentionally compact and limited to work currently in preparation or
        already presented.
      </p>
    </div>
  </header>

  <section class="section section--tight">
    <div class="container">
      <div class="section-head reveal">
        <p class="section-head__eyebrow">Manuscripts</p>
        <span class="section-head__line"></span>
      </div>
      <h2 class="section-title reveal">Current manuscripts</h2>

      <ul class="pub-list reveal">
        {manuscripts.map((item) => (
          <li>
            <p class="pub-authors" set:html={formatAuthors(item.data.authors)} />
            <p class="pub-title">{item.data.title}</p>
            <p class="pub-venue">{item.data.status ?? 'In preparation.'}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section section--tight">
    <div class="container">
      <div class="section-head reveal">
        <p class="section-head__eyebrow">Presentations</p>
        <span class="section-head__line"></span>
      </div>
      <h2 class="section-title reveal">Presentations and talks</h2>
      <ul class="pub-list reveal">
        {presentations.map((item) => (
          <li>
            <p class="pub-authors">{item.data.year}</p>
            <p class="pub-title">{item.data.title}</p>
            <p class="pub-venue">
              {item.data.venue}
              {item.data.status && ` · ${item.data.status}`}
            </p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section section--tight">
    <div class="container">
      <div class="section-head reveal">
        <p class="section-head__eyebrow">Recognition</p>
        <span class="section-head__line"></span>
      </div>
      <h2 class="section-title reveal">Recognition and support</h2>
      <ul class="pub-list reveal">
        {awards.map((item) => (
          <li>
            <p class="pub-authors">{item.data.year}</p>
            <p class="pub-title">{item.data.title}</p>
            <p class="pub-venue">{item.data.venue}</p>
          </li>
        ))}
      </ul>
      <div class="publications-note reveal">
        <p>This list is kept intentionally short and limited to current work.</p>
      </div>
    </div>
  </section>
</Base>
